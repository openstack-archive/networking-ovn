============
Installation
============

At the command line::

    $ pip install networking-ovn

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv networking-ovn
    $ pip install networking-ovn
